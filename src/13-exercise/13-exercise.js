import $ from "jquery";

function exercise13() {
  // Complete the code of the function
  $("button").click(function () {
    $(".box").innerWidth(400);
    $(".box").innerHeight(200);
  });
}

export default exercise13;
